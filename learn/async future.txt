非常好，这个顺序**完全正确**。
先**抛开 Tokio**，只从 **Rust 标准库语义**理解：

> async / await / Future / Stream
> 不然后面一切运行时（Tokio、async-std）都会变“玄学”。

我按：
**概念 → 真实语义 → 最小代码 → 心智模型 → 练习题**
一步步来。

---

# 一、先立住 4 个基本概念（一句话版）

> 1️⃣ `async fn`：
> 👉 不是函数，是**返回一个 Future 的工厂**。

> 2️⃣ `Future`：
> 👉 一个**可以被反复 poll 的状态机**。

> 3️⃣ `.await`：
> 👉 不是阻塞，是：
> “如果没好就 return Pending，把控制权让出去”。

> 4️⃣ `Stream`：
> 👉 Future 的“多次版”，
> 👉 async 版 Iterator。

---

# 二、Future 的真实含义（不是“将来会有个值”）

标准库里：

```rust
use std::future::Future;
use std::pin::Pin;
use std::task::{Context, Poll};

trait Future {
    type Output;
    fn poll(self: Pin<&mut Self>, cx: &mut Context)
        -> Poll<Self::Output>;
}
```

你必须抓住这一点：

> **Future = 一个“手写状态机”**
> poll 一次，走一步。
> Ready = 完成。
> Pending = 没好，下次再来。

---

# 三、async fn 到底返回了什么？

```rust
async fn foo() -> i32 {
    42
}

let fut = foo();
```

此时：

* `fut` 的类型是：`impl Future<Output = i32>`
* **什么都没发生**
* 没执行
* 没算 42

直到你：

```rust
fut.await
```

或有人去 poll 它。

---

# 四、.await 的真实语义（最关键）

```rust
let x = bar().await;
```

不是“阻塞等 bar 完成”。

而是**等价语义**：

```rust
loop {
    match bar_fut.poll(cx) {
        Poll::Ready(x) => break x,
        Poll::Pending => return Poll::Pending,
    }
}
```

也就是说：

* 当前函数（Future）暂停
* 把控制权交还调度器
* 等 waker 把我唤醒
* 下次从 `.await` 后继续

---

# 五、一个 async fn 会被编译成什么？

```rust
async fn foo() -> i32 {
    let x = bar().await;
    x + 1
}
```

编译器会变成类似：

```rust
enum FooState {
    Start,
    WaitingOnBar(BarFuture),
    Done,
}

struct Foo {
    state: FooState,
}

impl Future for Foo {
    type Output = i32;

    fn poll(self: Pin<&mut Self>, cx: &mut Context) -> Poll<i32> {
        match self.state {
            FooState::Start => {
                let bar_fut = bar();
                self.state = FooState::WaitingOnBar(bar_fut);
                return Poll::Pending;
            }

            FooState::WaitingOnBar(ref mut bar_fut) => {
                match bar_fut.poll(cx) {
                    Poll::Pending => Poll::Pending,
                    Poll::Ready(x) => {
                        self.state = FooState::Done;
                        Poll::Ready(x + 1)
                    }
                }
            }

            FooState::Done => panic!("polled after completion"),
        }
    }
}
```

**结论：**

> `async fn` = 自动生成状态机 + Future 实现。

---

# 六、async ≠ 并行 ≠ 多线程

这点你必须 100% 吃透。

```rust
async fn f() {
    heavy_cpu();        // ❌ 没有 .await
    println!("done");
}
```

这段代码：

* 不会让出 CPU
* 不会暂停
* 会一口气跑完

也就是说：

> async 是**协作式**，
> 没有抢占。

---

# 七、标准库里其实没有“执行器”

这一点很反直觉。

Rust 标准库：

* 定义了：

  * `Future`
  * `Poll`
  * `Context`
  * `Waker`
* **没有**：

  * 线程池
  * 调度器
  * 事件循环
  * runtime

也就是说：

> 标准库只给你“协议”，
> Tokio / async-std 才给你“发动机”。

---

# 八、手写一个最小 Future（眼见为实）

```rust
use std::future::Future;
use std::pin::Pin;
use std::task::{Context, Poll};

struct CountToThree {
    n: u32,
}

impl Future for CountToThree {
    type Output = ();

    fn poll(mut self: Pin<&mut Self>, _cx: &mut Context)
        -> Poll<()> 
    {
        if self.n < 3 {
            self.n += 1;
            println!("poll: {}", self.n);
            Poll::Pending
        } else {
            println!("done");
            Poll::Ready(())
        }
    }
}
```

如果你有一个最简 executor：

```rust
fn block_on<F: Future>(mut fut: F) {
    use std::task::{RawWaker, RawWakerVTable, Waker};

    fn dummy_raw_waker() -> RawWaker {
        fn no_op(_: *const ()) {}
        fn clone(_: *const ()) -> RawWaker { dummy_raw_waker() }
        let vtable = &RawWakerVTable::new(clone, no_op, no_op, no_op);
        RawWaker::new(std::ptr::null(), vtable)
    }

    let waker = unsafe { Waker::from_raw(dummy_raw_waker()) };
    let mut cx = Context::from_waker(&waker);

    let mut fut = unsafe { Pin::new_unchecked(&mut fut) };

    loop {
        match fut.as_mut().poll(&mut cx) {
            Poll::Ready(()) => break,
            Poll::Pending => {}
        }
    }
}
```

跑：

```rust
fn main() {
    block_on(CountToThree { n: 0 });
}
```

输出：

```
poll: 1
poll: 2
poll: 3
done
```

这一步非常重要：

> 你现在已经亲眼看到：
> Future = 被反复 poll 的状态机。

---

# 九、Stream 是什么？（标准库里还没 stabilized）

标准库**暂时没有** `Stream` trait（在 `futures-core` 里）。

概念对齐 Future：

```rust
trait Stream {
    type Item;
    fn poll_next(
        self: Pin<&mut Self>,
        cx: &mut Context
    ) -> Poll<Option<Self::Item>>;
}
```

* `Poll::Ready(Some(x))` → 产出一个值
* `Poll::Pending` → 现在没值
* `Poll::Ready(None)` → 结束

---

# 十、手写一个最小 Stream

```rust
struct Counter {
    cur: u32,
    max: u32,
}

impl Stream for Counter {
    type Item = u32;

    fn poll_next(
        mut self: Pin<&mut Self>,
        _cx: &mut Context,
    ) -> Poll<Option<u32>> 
    {
        if self.cur < self.max {
            let val = self.cur;
            self.cur += 1;
            Poll::Ready(Some(val))
        } else {
            Poll::Ready(None)
        }
    }
}
```

这和你刚刚写的 Future 几乎一模一样，只是“多次 Ready”。

---

# 十一、一个非常重要的心智模型

> async fn = 状态机构造器
> Future = 状态机实例
> .await = 状态机 yield 点
> Executor = poll 这些状态机的人
> Stream = 会多次 yield 的 Future

---

# 十二、你现在就能做的 6 个基础练习题（非常推荐）

这些练完，你对 async 语义会“肌肉记忆”。

---

## 练习 1：看输出顺序（核心题）

```rust
async fn foo() -> i32 {
    println!("foo start");
    let x = bar().await;
    println!("foo got {}", x);
    x + 1
}

async fn bar() -> i32 {
    println!("bar start");
    10
}

fn main() {
    let fut = foo();
    // 假设用 block_on 跑它
}
```

**问题：**

1. 哪些 println 什么时候执行？
2. 哪些是在 `.await` 之前？哪些之后？

---

## 练习 2：await 是不是“让出”？

```rust
async fn f() {
    println!("A");
    g().await;
    println!("B");
}

async fn g() {
    println!("C");
}
```

**问题：**

输出顺序是什么？
为什么不是 A B C？

---

## 练习 3：忘了 await 会怎样？

```rust
async fn foo() -> i32 {
    42
}

fn main() {
    let x = foo();
    println!("{:?}", x);
}
```

**问题：**

1. 能不能编译？
2. x 是什么类型？
3. 会不会打印 42？

---

## 练习 4：手写状态机

把这个：

```rust
async fn add_one() -> i32 {
    let x = async { 1 }.await;
    x + 1
}
```

**手写成一个 enum + impl Future**。

---

## 练习 5：Future 会不会自己跑？

```rust
async fn f() {
    println!("hi");
}

fn main() {
    let fut = f();
}
```

**问题：**

会不会打印 hi？
为什么？

---

## 练习 6：Stream vs Iterator

如果你有：

```rust
let mut s = my_stream();
while let Some(x) = s.next().await {
    println!("{x}");
}
```

**问题：**

1. `.next().await` 背后等价于什么 poll 逻辑？
2. 和 Iterator::next() 最大语义差异是什么？

---

# 十三、你现在这一步非常对味

你现在在做的这步：

> 先标准库
> 再 Tokio
> 再调度器
> 再高性能系统

这是**正确路线**。

---

# 如果你愿意，下一步我可以继续“补最后一块地基”：

**1️⃣ Pin / Unpin 到底是干什么的？**
（为什么 poll 要 `Pin<&mut Self>`？）

**2️⃣ Waker / Context 的真实职责**
（谁唤醒谁？怎么注册？）

这两块一吃透，你对 async 的理解就会从
“会用” → “能写 runtime”。

---------------------------------------------

非常好，现在这一步是**把“标准库语义”真正落到地上**。
你刚刚已经吃透了：

> Future = 状态机
> .await = yield
> 标准库没有 executor

现在我们**只加 Tokio 这一层发动机**，把一切连起来。

我按：
**Tokio 在补什么 → 真实执行路径 → .await 在 Tokio 里发生了什么 → 你会踩的坑 → 对齐你关心的系统设计**
来解。

---

# 一、Tokio 到底补了哪 3 样东西？

标准库只有“协议”，Tokio 补的是：

```
1) Executor（调度器）
2) Reactor（epoll/kqueue）
3) 任务系统（spawn / waker / 队列）
```

一句话：

> Tokio =
> “poll Future 的人” + “等 IO 的人” + “唤醒 Future 的人”。

---

# 二、从 main 开始：Tokio Runtime 在干什么？

```rust
#[tokio::main]
async fn main() {
    foo().await;
}
```

宏展开后本质是：

```rust
fn main() {
    let rt = tokio::runtime::Runtime::new().unwrap();
    rt.block_on(async {
        foo().await;
    });
}
```

也就是说：

> Tokio 先建一个 runtime，
> 然后用 block_on 开始 poll 你的 main Future。

---

# 三、一条 Future 在 Tokio 里完整跑一圈

我们用这段最小真实代码：

```rust
#[tokio::main]
async fn main() {
    let x = foo().await;
    println!("result = {}", x);
}

async fn foo() -> i32 {
    println!("foo start");
    let y = bar().await;
    println!("foo got {}", y);
    y + 1
}

async fn bar() -> i32 {
    println!("bar start");
    10
}
```

---

## Step 1：spawn main Future

```text
Tokio Runtime
  → 把 main() 的 async block 包成 Task
  → push 到 worker 的 run queue
```

---

## Step 2：Worker 线程开始 poll

```text
worker_loop:
  pop task
  poll(task.future)
```

进入：

```text
poll(main future)
  → 进入 foo().await
```

---

## Step 3：poll foo()

```text
poll(foo future)
  → println!("foo start")
  → 遇到 bar().await
```

---

## Step 4：poll bar()

```text
poll(bar future)
  → println!("bar start")
  → 立刻返回 Poll::Ready(10)
```

---

## Step 5：恢复 foo()

```text
bar().await 得到 10
→ println!("foo got 10")
→ foo 返回 Poll::Ready(11)
```

---

## Step 6：恢复 main()

```text
foo().await 得到 11
→ println!("result = 11")
→ main 返回 Poll::Ready(())
```

---

**注意：**

> 整个过程
> 没有线程切换
> 没有 IO
> 没有 Pending
> 只是一口气 poll 完。

---

# 四、真正有 IO 时：.await 在 Tokio 里发生了什么？

现在换成真实 IO：

```rust
use tokio::net::TcpStream;
use tokio::io::{AsyncReadExt, AsyncWriteExt};

async fn handle(mut s: TcpStream) {
    let mut buf = [0; 1024];
    let n = s.read(&mut buf).await;
    s.write_all(&buf[..n]).await;
}
```

---

## Step 1：第一次 poll 到 read().await

```text
poll(handle future)
  → 进入 s.read().await
  → 调用 TcpStream 的 poll_read()
```

---

## Step 2：socket 现在没数据

```text
poll_read()
  → 注册 waker 到 Reactor
  → return Poll::Pending
```

这一步非常关键：

> Future 不再往下走
> 任务暂停
> Tokio 线程立刻去跑别的任务。

---

## Step 3：Reactor 在干什么？

Tokio 内部 Reactor 线程：

```text
epoll_wait()
  → fd 可读
  → 找到对应 waker
  → waker.wake()
```

---

## Step 4：waker.wake()

```text
wake()
  → 把 handle task push 回某个 worker 的 run queue
```

---

## Step 5：任务被再次 poll

```text
worker pop handle task
  → poll(handle future)
  → 从 s.read().await 后继续
```

---

## Step 6：继续往下执行

```text
得到 n
→ s.write_all(...).await
→ 可能再次 Pending
→ 重复上面的流程
```

---

# 五、你现在可以精确理解这句话了

> `.await` 不是阻塞线程
> 而是：
> “如果没准备好，就 return Pending，
> 把我挂起，等 waker 再 poll 我”。

---

# 六、Tokio 调度器在干什么？（对齐你刚刚懂的）

Tokio 的 worker 线程本质：

```text
loop {
    task = pop_local_queue() or steal()
    poll(task.future)
}
```

你现在已经非常熟这个模型了：

| 你刚学的           | Tokio          |
| -------------- | -------------- |
| 手写 block_on    | Tokio executor |
| poll(Future)   | Worker 线程      |
| return Pending | 任务让出 CPU       |
| waker.wake()   | 任务重新入队         |

---

# 七、spawn / join 在 Tokio 里到底是什么？

---

## 7.1 tokio::spawn

```rust
let h = tokio::spawn(async {
    foo().await
});
```

本质：

```text
spawn(fut)
  → Task { future, state }
  → push 到 run queue
```

它**不会立刻执行**，只是：

> 把 Future 丢给调度器。

---

## 7.2 等待子任务：JoinHandle

```rust
let x = h.await?;
```

语义上等价于：

```text
loop {
    if child task done {
        return result
    } else {
        register waker
        return Pending
    }
}
```

---

# 八、并发 vs 串行 await（你现在能精确解释了）

---

## 串行：

```rust
let a = foo().await;
let b = bar().await;
```

时间：

```
foo 完 → 再 bar
```

---

## 并发：

```rust
let (a, b) = tokio::join!(foo(), bar());
```

或：

```rust
let a = tokio::spawn(foo());
let b = tokio::spawn(bar());

let a = a.await?;
let b = b.await?;
```

语义：

```text
spawn foo → 入队
spawn bar → 入队

worker 轮流 poll 两个 task
谁 Pending 谁让出
```

---

# 九、你一定会踩的 4 个 Tokio 级坑（不是标准库级）

这 4 个坑正好对齐你在抠的 gateway / 高性能链。

---

## 1️⃣ 在 async 里做阻塞

```rust
std::thread::sleep(1);   // ❌
tokio::time::sleep(1).await; // ✅
```

否则：

> 卡死整个 worker 线程。

---

## 2️⃣ 在 async 里跑 CPU 重活

```rust
heavy(); // ❌
tokio::spawn_blocking(|| heavy()); // ✅
```

否则：

> 其他任务全部饿死。

---

## 3️⃣ 一个 task 跑太久不 .await

```rust
loop {
    do_cpu_work(); // ❌ 没有 yield
}
```

否则：

> Tokio 无法抢占你。

---

## 4️⃣ 忘了 .await = 任务没跑

```rust
foo();        // ❌
foo().await;  // ✅
```

---

# 十、把 Tokio 精确对齐你前面熟的模型

你前面已经很熟：

| 你熟的                 | Tokio 对应        |
| ------------------- | --------------- |
| Netty EventLoop     | Tokio worker    |
| epoll               | Reactor         |
| handler 回调          | Future::poll    |
| fireChannelRead     | waker.wake      |
| Pipeline            | async 调用链       |
| Rayon work-stealing | Tokio scheduler |

---

# 十一、你现在应该有的“完整心智模型”

> async fn = 状态机构造器
> Future = 状态机实例
> .await = yield 点
> Tokio = poll 状态机的人
> Reactor = 等 IO 的人
> Waker = 重新排队的钩子

---

# 十二、给你 4 个 Tokio 级小练习（非常推荐）

这 4 题一做，你会“彻底不迷糊”。

---

## 练习 1：输出顺序（Tokio 版）

```rust
#[tokio::main]
async fn main() {
    println!("A");
    tokio::spawn(async {
        println!("B");
    });
    println!("C");
}
```

**问题：**

1. 输出顺序可能是什么？
2. 为什么不是固定顺序？

---

## 练习 2：并发 vs 串行

```rust
async fn f() {
    tokio::time::sleep(Duration::from_secs(1)).await;
}

#[tokio::main]
async fn main() {
    f().await;
    f().await;
}
```

vs

```rust
#[tokio::main]
async fn main() {
    tokio::join!(f(), f());
}
```

**问题：**

1. 两段代码各耗时多久？
2. 为什么？

---

## 练习 3：spawn vs 不 spawn

```rust
async fn f() {
    println!("hi");
}

#[tokio::main]
async fn main() {
    let h = tokio::spawn(f());
    println!("bye");
    h.await.unwrap();
}
```

**问题：**

1. hi 和 bye 谁先打印？
2. 为什么不确定？

---

## 练习 4：阻塞会发生什么？

```rust
#[tokio::main]
async fn main() {
    tokio::spawn(async {
        loop {
            println!("tick");
            std::thread::sleep(Duration::from_secs(1)); // ❌
        }
    });

    tokio::spawn(async {
        loop {
            println!("tock");
            tokio::time::sleep(Duration::from_secs(1)).await;
        }
    });
}
```

**问题：**

1. 你会看到 tick / tock 都正常吗？
2. 为什么？

---

# 十三、你现在这一步非常对味（而且很高级）

你现在的路线是：

```
标准库语义
   ↓
Tokio runtime
   ↓
调度器 / Reactor
   ↓
高性能系统设计
```

这正是：

> 从“会用 async”
> 到“能设计 async runtime”。

---

# 如果你愿意，下一步我可以继续抠一层你一定会喜欢的：

**1️⃣ Tokio 的调度器源码级结构（worker / run queue / steal）**
（和 Rayon 逐行对齐）

**2️⃣ 一条真实 Tokio IO 请求路径**
从 epoll → waker → Future → .await 恢复

这两条，直接对齐你现在在抠的 Hyper / gateway / 高性能链并发模型。


-----------------------------------------------------
很好，这一层**正好对齐你前面抠 Rayon / Monad / Netty 的调度器直觉**。
我按**源码级结构 + 执行路径 + 和 Rayon 的逐点对齐**给你拆。

> 目标：
> 你看完后，能在脑子里“单步跑” Tokio worker 的 while 循环。

---

# 一、Tokio 调度器的三种模式（先定范围）

Tokio runtime 有 3 种 scheduler：

1. `current_thread`（单线程）
2. `multi_thread`（默认，多 worker + work-stealing）
3. `multi_thread_alt`（老实现，基本淘汰）

你关心的一定是 **multi_thread**。

源码入口（tokio 1.x）：

```
tokio/src/runtime/
  ├── scheduler/
  │     ├── multi_thread/
  │     │     ├── worker.rs
  │     │     ├── queue.rs
  │     │     ├── park.rs
  │     │     ├── handle.rs
  │     │     └── mod.rs
  │     └── current_thread/
```

---

# 二、核心结构体（最重要的 4 个）

你只要抓住这 4 个就行了：

---

## 1️⃣ Worker

```rust
struct Worker {
    run_queue: LocalQueue<Task>,
    steal: StealQueue<Task>,
    parker: Parker,
    core: Core,
}
```

语义：

| 字段        | 干嘛的       |
| --------- | --------- |
| run_queue | 本线程的本地队列  |
| steal     | 给别人偷的队列   |
| parker    | 没任务时 park |
| core      | 当前线程的调度状态 |

---

## 2️⃣ LocalQueue / StealQueue

```rust
struct LocalQueue<T> {
    inner: queue::Local<T>
}

struct StealQueue<T> {
    inner: queue::Steal<T>
}
```

这是一个 **Chase-Lev 双端队列**（和 Rayon 同类）：

* 自己 `push/pop` 走一端（快、无锁）
* 别人 `steal` 走另一端（慢、原子）

---

## 3️⃣ Task

```rust
struct Task {
    header: Header,
    future: UnsafeCell<Option<Future>>,
}
```

Header 里有：

```rust
struct Header {
    state: AtomicUsize,
    vtable: &'static TaskVTable,
    scheduler: *const (),
}
```

重点：

> 每个 spawn 出来的 Future
> 都被包成一个 Task
> 带原子状态机。

---

## 4️⃣ Scheduler / Handle

```rust
struct MultiThread {
    workers: Vec<Worker>,
    injector: Injector<Task>,
}
```

| 字段       | 干嘛的                |
| -------- | ------------------ |
| workers  | 所有 worker 线程       |
| injector | 全局注入队列（跨线程 push 用） |

---

# 三、Worker 主循环（这是 Tokio 的“心跳”）

你可以把它想成：

```rust
fn worker_loop(worker: &mut Worker) {
    loop {
        if let Some(task) = worker.run_queue.pop() {
            poll_task(task);
            continue;
        }

        if let Some(task) = scheduler.injector.pop() {
            worker.run_queue.push(task);
            continue;
        }

        if let Some(task) = steal_from_other_worker(worker) {
            worker.run_queue.push(task);
            continue;
        }

        // 没活了，park 等 waker
        worker.parker.park();
    }
}
```

真实代码在：

```
tokio/src/runtime/scheduler/multi_thread/worker.rs
```

逻辑一模一样，只是多了：

* batch steal
* fairness
* cooperative budget

---

# 四、spawn 一条任务：源码级路径

你现在关心的一定是：

> `tokio::spawn(fut)`
> 究竟把任务塞到哪了？

---

## Step 1：构造 Task

```rust
let task = Task::new(future);
```

---

## Step 2：提交给调度器

```rust
handle.spawn(task);
```

在 `handle.rs` 里：

```rust
fn spawn(&self, task: Task) {
    if self.is_local_thread() {
        self.worker.run_queue.push(task);
    } else {
        self.injector.push(task);
        self.notify_one_worker();
    }
}
```

---

## Step 3：某个 worker 拿到 task

```text
worker loop
  → pop_local()
  → pop_injector()
  → steal()
```

---

# 五、poll 一条任务：Task::poll 路径

这是 Tokio 和你刚学的 Future 协议对齐的地方。

---

## Step 1：worker 取到 task

```rust
let task = worker.run_queue.pop().unwrap();
```

---

## Step 2：进入 Task::poll

```rust
task.poll(&mut cx);
```

简化后：

```rust
fn poll(task: &Task, cx: &mut Context) {
    if !task.transition_to_running() {
        return;
    }

    match task.future.poll(cx) {
        Poll::Ready(_) => task.complete(),
        Poll::Pending => task.transition_to_idle(),
    }
}
```

---

## Step 3：Future::poll 里注册 waker

如果 Future Pending：

```text
poll_read()
  → reactor.register(fd, waker)
  → return Pending
```

---

## Step 4：任务挂起

```text
task state = IDLE
不在任何队列里
```

---

# 六、waker.wake()：把 Task 重新丢回队列

这是 Tokio 和 epoll 接起来的那一钩。

---

## Step 1：Reactor 线程发现事件

```text
epoll_wait()
  → fd ready
  → 找到 waker
  → waker.wake()
```

---

## Step 2：waker.wake()

```rust
fn wake(&self) {
    if task.transition_to_scheduled() {
        scheduler.injector.push(task);
        scheduler.notify_one_worker();
    }
}
```

关键点：

> wake 并不 poll
> 只做一件事：
> 把 Task 丢回队列。

---

# 七、Steal 路径（你最关心的）

当本 worker 没活：

```rust
fn steal_from_other_worker(worker: &Worker) -> Option<Task> {
    for other in workers.iter().random_order() {
        if let Some(task) = other.steal.steal() {
            return Some(task);
        }
    }
    None
}
```

语义：

```
我本地没活了
→ 从别人 steal 一半任务
→ push 到我本地队列
```

---

# 八、Cooperative Scheduling（Tokio 的“软抢占”）

这是 Tokio 和 Rayon 最大的设计差异之一。

---

## 8.1 budget 概念

```rust
struct Budget {
    remaining: usize,
}
```

每个 task poll 时带一个预算：

```text
每次 poll 消耗 1
如果耗尽 → 强制 return Pending
```

---

## 8.2 为什么要这个？

避免：

```rust
async fn bad() {
    loop {
        do_cpu_work(); // 没 .await
    }
}
```

Tokio 的防御：

> 就算你没 .await
> Tokio 也会强制 yield。

---

# 九、和 Rayon 的逐点对齐（你会觉得很熟）

| Rayon           | Tokio         |
| --------------- | ------------- |
| Worker deque    | LocalQueue    |
| StealDeque      | StealQueue    |
| global injector | Injector      |
| spawn(job)      | spawn(task)   |
| work stealing   | work stealing |
| job.run()       | task.poll()   |
| no IO           | 有 Reactor     |
| 无 waker         | 有 waker       |
| 无 Future        | Future 状态机    |

---

# 十、你现在可以“单步跑”这条真实路径了

我给你一条从 spawn → steal → IO → wake → 再 poll 的完整时序：

---

## 场景

```rust
tokio::spawn(async {
    let n = socket.read(&mut buf).await;
    println!("{}", n);
});
```

---

### T0：spawn

```text
main thread
→ Task 构造
→ push 到 injector
```

---

### T1：worker-1 拿到任务

```text
worker-1 pop injector
→ poll(task)
→ 进入 read().await
→ poll_read() = Pending
→ 注册 waker
→ task state = IDLE
```

---

### T2：worker-1 没活了，去 steal

```text
worker-1 本地队列空
→ steal worker-2 的一半任务
```

---

### T3：socket 可读

```text
Reactor
→ epoll_wait 返回
→ 找到 waker
→ waker.wake()
→ push task 到 injector
```

---

### T4：worker-3 拿到任务

```text
worker-3 pop injector
→ poll(task)
→ read 完成
→ println!
→ task complete
```

---

# 十一、你现在这一步已经是“runtime 设计层”

你现在问的这层，其实已经是：

> “我如果要自己写一个 Tokio / Monad / Hyper runtime，
> 这个 scheduler 至少要长这样。”

---

# 十二、如果你愿意，下一步可以继续抠两条你一定感兴趣的：

1️⃣ Tokio 的 Reactor 结构（epoll → waker → task）
（和 Netty NIO / Hyper 的 event loop 逐点对齐）

2️⃣ Tokio 的 Task 原子状态机
（为什么不会 double poll / double wake）

这两条刚好对齐你最近在抠的：
**高性能 gateway / 共识节点的 IO 并发模型。**
